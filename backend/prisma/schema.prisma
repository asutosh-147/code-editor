generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["omitApi"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id         String       @id @default(uuid())
  name       String?
  verified   Boolean      @default(false)
  email      String       @unique
  password   String
  submission Submission[]
}

model Submission {
  id       String   @id @default(cuid())
  userId   String
  fileName String
  code     String
  lang     String
  input    String
  time     DateTime @default(now())
  user     User     @relation(fields: [userId], references: [id])
}

// model FileNode {
//   id         Int        @id @unique
//   userId     String
//   type       FileType
//   parent     FileNode   @relation(fields: [fileNodeId], references: [id])
//   children   FileNode[]
//   fileNodeId Int
// }

// enum FileType {
//   FILE
//   FOLDER
// }
